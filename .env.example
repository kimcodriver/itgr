# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
# Server-only — never exposed to client. Used by BFF server actions.
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# App
NEXT_PUBLIC_APP_NAME="ITGR Audit Tracker"
APP_ENV=development
